using System;

namespace PrimeraAplicacion
{
    class Program
    {
        static void Main(string[] args)
        {
            //Console.WriteLine("Hola este es el curso de C#");

            //declarar variables
            /*int edad=0;
            for (int i = 0; i < 10; i++)
            {
                edad += 2;
                Console.WriteLine($"La variable es igual a {edad}"); //interpolación
            }

            //Conversión explícita (casting)
            double tem = 35.7;
            int temp = (int)tem;

            //conversión implícita --- Datos compatibles
            int hab = 10;
            long ciud2018 = hab;

            float peso = 5.8F;
            double p = peso; */

            //Conversion de texto a número
            /*
            Console.Write("Ingrese número: ");
            int num1 = int.Parse(Console.ReadLine());

            Console.Write("Ingrese número: ");
            int num2 = int.Parse(Console.ReadLine());

            Console.WriteLine($"Suma = {num1 + num2}");
            */

            //Constantes
            //const int VALOR = 34;

            //Métodos
            //suma(5, 3);
        }

        /*
        public static void suma(int num1, int num2)
        {
            Console.WriteLine(num1 + num2);

        }
        */
    }
}
